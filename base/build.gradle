description = "grammars for parsing BASE sytaxes"

generateGrammarSource { //antlr4
    outputDirectory = new File("${project.buildDir}/generated-src/antlr/main/org/openehr/baseparser".toString())
    arguments = [
            '-package', 'org.openehr.baseparser'
    ] + arguments
}

classes {
    dependsOn generateGrammarSource
}
